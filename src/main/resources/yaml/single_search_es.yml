queries:
  - name: caseOverview
    index:
      - cases
    filter:
      type: pagination
      defaultSortField: case_id
      alternativeSortField:
        subject_id: case_id
      rangeFilterFields:
        - dx_age
        - dx_ages
        - enrollment_age
        - enrollment_ages
        - enrollment_year
        - enrollment_years
    result:
      type: object_array